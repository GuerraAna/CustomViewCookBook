<?xml version="1.0" encoding="utf-8"?>
<manifest
	xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:tools="http://schemas.android.com/tools">

	<application
		android:allowBackup="true"
		android:dataExtractionRules="@xml/data_extraction_rules"
		android:fullBackupContent="@xml/backup_rules"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.CustomViewCookBook">

		<!--  Home Feature. Responsible to initialize the application.  -->
		<activity
			android:name=".home.HomeActivity"
			android:exported="true">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<!--  Banner Feature.  -->
		<activity
			android:name=".features.components.banner.BannerActivity"
			android:exported="false" />

		<!--  Intent Activities Feature.	-->
		<activity
			android:name=".features.navigation.FirstActivity"
			android:exported="false" />
		<activity
			android:name=".features.navigation.SecondActivity"
			android:exported="false" />
		<activity
			android:name=".features.navigation.ThirdActivity"
			android:exported="false" />
	</application>

</manifest>